# mock-plugin

1. Clone `cc-py-sdk` into `mock-plugin/`

https://github.com/USACE-Cloud-Compute/cc-py-sdk

2. Build image for use with the `featapi`

```bash
docker build -f mock-plugin/export.Dockerfile -t cc-stormsim-plugin-test .
```
